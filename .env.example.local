# Example env file for running the API outside Docker (local dev)
#
# Usage:
#   cp .env.example.local .env.local
#   export $(grep -v '^#' .env.local | xargs)
#   uvicorn app.main:app --reload
#
# Or let Docker Compose load it:
#   docker compose --env-file .env.local up

# Postgres connection (local)
DATABASE_URL=postgresql://cyber:cyberpass@localhost:5432/cybergrader
DATABASE_SCHEMA=public

# Optional: metadata for logs only
# CONTENT_REFRESH_SCHEDULE=nightly
# DATABASE_BACKUP_SCHEDULE=nightly
